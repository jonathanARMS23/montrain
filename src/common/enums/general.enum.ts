export enum ClassType {
    Firstclass = 'firstclass',
    Secondclass = 'secondclass',
}

export enum State {
    Confirmed = 'confirmed',
    Canceled = 'Canceled',
}

export enum Role {
    Client = 'client',
    Admin = 'admin',
}
